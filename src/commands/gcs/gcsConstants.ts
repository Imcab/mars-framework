import * as path from 'path';

export const GCS = {

    FOLDER: path.join('mars_terminal', 'Release'),
    EXE: 'mars_gcs.exe',
    
    REPO_URL: 'https://api.github.com/repos/Imcab/MarsGCS/releases/latest',
    ZIP_FILE: 'mars_terminal.zip'
};